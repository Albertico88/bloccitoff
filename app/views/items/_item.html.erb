<tr>
  <td>
    <%= item.name %>
  </td>
  <td>
   <%= distance_of_time_in_words(item.created_at + 7.days, Time.now) %>
  </td>
  <td>
    <%= link_to "Completed", user_item_path(current_user, item), method: :delete, class: "btn btn-success" %>
  </td>
</tr>
